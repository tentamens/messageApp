import 'package:flutter/material.dart';

class HomePageMessages extends StatefulWidget {
  const HomePageMessages({Key? key}) : super(key: key);

  @override
  _HomePageMessagesState createState() => _HomePageMessagesState();
}

class _HomePageMessagesState extends State<HomePageMessages> {
  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemBuilder: (context, index) {
        return Container();
      },
    );
  }
}
